name: bb-touch-bot
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - numpy  
  - requests
  - ta-lib
  - aiohttp
  - redis-py>=4.2.0  # Use conda version instead of pip
